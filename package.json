{
  "name": "pdf-to-quiz-root",
  "private": true,
  "description": "Asıl uygulama pdf-quiz/ altında; kökten komutlar oraya yönlendirilir.",
  "scripts": {
    "dev": "npm run dev --prefix pdf-quiz",
    "build": "npm run build --prefix pdf-quiz",
    "lint": "npm run lint --prefix pdf-quiz",
    "preview": "npm run preview --prefix pdf-quiz"
  }
}
